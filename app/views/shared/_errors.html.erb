<% if movie.errors.any? %>


 <%= movie.errors.full_messages.to_sentence %> 
<section>
<h2>Opps! Your form could not be saved.</h2>

<h3>Please correct the following</h3> 

<%= pluralize(movie.errors.size, "errors") %>: 
<ul>

<%= movie.errors.full_messages.each do |message| %>  
<li><%= message %></li>

<% end %> 

</ul>

</section> 

<% end %>